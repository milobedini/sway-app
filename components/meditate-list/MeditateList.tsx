import { Text } from "react-native";
import Animated from "react-native-reanimated";

export const MeditateList = (): JSX.Element => (
  <Animated.ScrollView>
    <Text>Meditate List Component</Text>
  </Animated.ScrollView>
);
